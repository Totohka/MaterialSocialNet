module.exports = {
  /**
   * projectName - имя проекта
   *
   * buildPath - путь по которому находятся файлы после билда
   *
   * outputPath = финальный путь, где будут находится файлы
   * */
  config: {
    projectName: 'soc-net-web',
    buildPath: 'production',
    outputPath: 'publish',
  },
};
