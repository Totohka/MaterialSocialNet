export type SendNotificationReturn = {
  id: number;
};

export type NotificationPostReturn = {
  firstName: string;
  lastName: string;
  userId: number;
  postId: number;
};

export type NotificationChatReturn = {
  chatName: string;
  lastMessage: string;
  chatId: number;
};
